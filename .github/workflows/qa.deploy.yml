name: QA · Deploy (AWS)

on:
  push:
    branches: [release]

jobs:
  not-available:
    name: QA environment not yet available
    runs-on: ubuntu-latest
    steps:
      - run: echo "QA environment pending configuration."
        shell: bash
